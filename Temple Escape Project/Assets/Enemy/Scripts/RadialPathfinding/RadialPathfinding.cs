using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RadialPathFinding : MonoBehaviour
{
    //Purpose: Attached to the enemy running separately and returns current resultant direction and cartestian format to the enemy to navigate. 

    //Properties
    /*
     * Attach to enemy
     * Radius = constant * nth difficulty
     * Tree of map
     * List of rooms
     * Detection rate?
     * Current resultant direction
     */
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //Update resultant direction
    //Convert into vector into cartesian (x,y)
    //Get connected rooms
    
}
